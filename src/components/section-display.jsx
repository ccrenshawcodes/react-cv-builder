export default function SectionDisplay ({ data }) {
    //  remember in this case data is an ARRAY with an OBJECT nested within it
    //  so we will have to go array item by array item
    //  then object item by object item
    //  and somehow return some jsx from all of this
    //  console.log(data);

}

//  this should elegantly accommodate additions of an array item for education or experience